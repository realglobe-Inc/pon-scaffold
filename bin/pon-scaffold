#!/usr/bin/env node

/**
 * Command line interface of pon-scaffold.
 * This file is auto generated by ape-tmpl.
 */

'use strict'

const program = require('commander')
const pkg = require('../package')
const ponScaffold = require('../lib')

program
  .version(pkg[ 'version' ])
  .usage('[options] <type> <dest> ')
  .description(pkg[ 'description' ])
  .option('-s, --silent', 'Disable console logs')
  .option('-f, --force', 'Force to generate')

// Show examples
program.on('--help', () => {
  console.log('  Examples:')
  console.log('')
  console.log('  $ pon-scaffold task my-awesome-task   # Generate a task project')
  console.log('')
})

program.parse(process.argv)

// Run main command
ponScaffold.apply(ponScaffold, program.args.concat({
  silent: program.silent,
  force: program.force})
).catch(handleError)

// Handlers

/** Handle error */
function handleError (err) {
  console.error(err)
  process.exit(1)
}
