/**
 * Test for server.
 * Runs with mocha.
 */
'use strict'

const server = require('../server')
const { ok, equal } = require('assert')

describe('server', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
