/**
 * Test for server.
 * Runs with mocha.
 */
'use strict'

const {equal, ok} = require('assert')
const server = require('../server')

describe('server', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
  })
})

/* global describe, before, after, it */
