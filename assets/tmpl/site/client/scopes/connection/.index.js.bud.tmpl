'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  name: 'scopes',
  cjs: true,
  description: 'scopes'
})

if (!module.parent) {
  require('coz').render(__filename)
}