/**
 * Render markdowns
 */
'use strict'

const { markdowns } = require('the-templates')
const markdownDir = `${__dirname}/../../../assets/markdowns`

module.exports = markdowns({
  dirname: markdownDir
})

if (!module.parent) {
  require('coz').render(__filename)
}