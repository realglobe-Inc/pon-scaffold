'use strict'

const { dir } = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  name: 'mixins',
  description: 'Scene mixin functions',
  node: true
})

if (!module.parent) {
  require('coz').render(__filename)
}