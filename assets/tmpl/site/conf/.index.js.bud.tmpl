'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  cjs: true,
  name: 'conf',
  description: 'Project configurations'
})

if (!module.parent) {
  require('coz').render(__filename)
}